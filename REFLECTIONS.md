The array method push() adds new elements to the end of an array, and returns the new length, and filter() creates a new array with every element in an array that pass a test.

A challenge I faced when trying to filter and search for items in the array was that I had to convert the items to lowercase just in case the user typed the input as upper otr mixed case.

How would you modify this code to make it more efficient or user-friendly? Indicate which items were removed and give thee user a chance to add them back if they had accidentally removed them from the list.
